node_modules/
dist/
dist-single/
*.log

# Capacitor native projects are generated with `npx cap add ios|android`.
# They are large and platform-specific; regenerate locally rather than commit.
/ios/
/android/
.DS_Store
